import { Typography } from "@mui/material";

const ContactInfo = () => {
  return <div>
    <Typography variant="h1">Contact me.</Typography>
    <div>LinkedIn resume Github</div>
  </div>;
};

export default ContactInfo;
