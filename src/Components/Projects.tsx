import { Typography } from "@mui/material";

const Projects = () => {
    return (<div>
        <Typography variant="h1">Things I've done.</Typography>
        <Typography>Amara</Typography>
        <Typography>Lord Fairfax</Typography>
        <Typography>coolbot</Typography>
    </div>)
}

export default Projects;